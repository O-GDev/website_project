This is a Vite + React scaffold that mirrors the original Flexibank site.

Notes:
- The app references existing assets in `../Flexibank/assets` to preserve styles and scripts.
- Contact form posts to `/Flexibank/sendemail.php` to keep backend behavior unchanged.

Commands:
- `npm install` then `npm run dev` to start development.
